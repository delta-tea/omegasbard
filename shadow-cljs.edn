{:deps     {:aliases [:dev]}

 :nrepl    {:port       9000
            :middleware []} ; optional list of namespace-qualified symbols

 :dev-http {8080 "public"}

 :builds   {:frontend {:target  :browser
                       :modules {:main {:init-fn omegasbard.core/init}}}}}
